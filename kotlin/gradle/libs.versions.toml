# Project-specific dependencies
# Shared versions (kotlin, koin, koin-plugin) are in ../gradle/libs.versions.toml

[versions]
mockito = "4.8.0"

[libraries]
mockito = { module = "org.mockito:mockito-inline", version.ref = "mockito" }
